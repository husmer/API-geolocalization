"	var apiResponse struct {
		ArtistsWithRelations []data_structs.ArtistWithRelations `json:"artistsWithRelations"`
	}
	err = json.NewDecoder(response.Body).Decode(&apiResponse)
	if err != nil {
		http.Error(w, err.Error(), http.StatusInternalServerError)
		return
	}

	// Use the FilterArtists function to filter artists
	matchingArtists := helpers.FilterArtists(query, apiResponse.ArtistsWithRelations)"

1. matchingArtists sisaldab kõikide asukohtade nimesid mis tekkisid artisti otsingus.
2. kasuta geocodingut et saada see kaart ja muuta asukoha string koordinaatideks.
3. pane kõik kontserdite asukohad kaardile
4. kõik see tuleb kirjutada funktsiooni SearchArtist, et search http template tooks ta nähtavale

// Probleemid //
1. välja tuleb mõelda mida teha siis kui mitu artisti on otsingu tulemus (if len(matchingArtists[0]) > 1 then ära näita geolocalizationi)


****HINTS

Add the API key to your request

You must include an API key with every Maps Embed API request. In the following example, replace YOUR_API_KEY with your API key.

  https://www.google.com/maps/embed/v1/MAP_MODE?key=YOUR_API_KEY&parameters
